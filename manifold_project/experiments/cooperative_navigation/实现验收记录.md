# 合作导航：实现验收记录

更新：2026-09-22。本页记录工程验证；学习表现与结论只见[当前实验判断](results/当前实验判断.md)。首达长预算训练尚未启动。

## 1. 环境与来源

Windows，解释器D:/python_exercise_program/dqn+pso/.conda/python.exe，PyTorch 2.7.1+cu118，RTX 4060 Laptop GPU；数值float32、回合统计float64。使用既有KMP_DUPLICATE_LIB_OK兼容设置和CUBLAS_WORKSPACE_CONFIG=:4096:8。

MAPPO/IPPO共享固定作者提交de66d7a4b23fac2513f56f96f73b3f5cb96695ac，vendor算法文件还原导入命名空间后SHA256与UPSTREAM记录一致。本次没有改写作者PPO、GAE或ValueNorm算法。

## 2. 回归范围

完整52项回归已通过，包括旧finite_horizon/continuing兼容与新first_arrival。随后针对边界和输出文案的小调整复验首达专项。

| 范围 | 已验证内容 |
|---|---|
| 任务终点 | 首达即停；截止失败真终止；最后奖励保留；截止步成功优先；初始成功零步 |
| 时钟/配额 | task_horizon独立于训练配额；批末未完成片段保留真实最终时钟与观测 |
| MC/GAE | 真终止无bootstrap，采样截断bootstrap；不串入reset后的奖励 |
| 变长数据 | valid/lengths与实际步数一致；padding不参与目标、损失及诊断 |
| 作者PPO | CPU/CUDA真实数据打包的GAE与独立逐回合计算一致；适配更新与直接作者trainer逐参数一致 |
| IPPO信息边界 | 删除集中状态字段仍能更新；各机器人保留自己的本地价值 |
| 恢复 | 8个条件在首达CPU提交边界恢复与连续训练的网络、账目、曲线一致；原作者PPO的CPU/CUDA恢复回归保留 |
| 冻结迁移 | N=3/4/6/8可执行，参数不变，N=4复用源评价，重跑复用缓存 |
| 图表/统计 | 主指标换为成功率与截尾步数；best排序、评价实际成本、CSV和总览可生成 |
| 稳定性/分支 | 首达高/低平台与节点不足区分；no_checks同任务分支保留新评价口径 |
| GIF | 新任务遇success停止，不生成成功后动作；结束原因写入末帧 |

测试入口：

```powershell
$env:KMP_DUPLICATE_LIB_OK = 'TRUE'
$env:CUBLAS_WORKSPACE_CONFIG = ':4096:8'
python -m unittest discover -s manifold_project/experiments/cooperative_navigation/tests -v
```

## 3. 实际命令短程验证

使用新协议177维Actor、128隐藏宽度、task_horizon=500和CUDA，在临时目录完成：

- 作者MAPPO/IPPO各1600源步，保留作者MPE配置的10 epochs；N-C/N-T、监控、总览、已完成套件恢复及独立首达评价。
- ours/no_checks短程训练；ours此次方向拒绝，实际采5600步，no_checks执行8轮共12800步，均不超过各自13600测试预算。此处仅检验执行，不解释性能。
- IPPO在N=8的500步上限GIF输出，文件有效；新任务首达提前停止另有专门回归。
- 恢复/评价前后final摘要一致，仅保留best/final；临时目录自动清理。
- 运行说明中的作者PPO、ours/no_checks、GAE诊断、单模块补充命令均通过2000万步配置dry-run。

## 4. 真实旧模型诊断

另对已有20M作者MAPPO/IPPO各评价200场景，首达或500步截止即停，生成episodes.csv、summary.csv和overview.png；评价前后final摘要一致。随后按用户清理要求删除旧持续任务训练产物，将三份诊断原样移至results/legacy_ppo_arrival_s40_h500，并用一份provenance.json保留来源配置、训练汇总和摘要。删除前再次核对模型摘要，移动前后三份诊断摘要一致。原模型已不在工作区，不能直接复跑历史评价。

以上测试不保证2000万步收敛、跨设备逐位复现、empirical检查安全或零样本迁移优势。新的正式结论仍需新协议长训练和独立种子。
